[1mdiff --git a/app/api/v1/deps.py b/app/api/v1/deps.py[m
[1mindex e157304..fd44b48 100644[m
[1m--- a/app/api/v1/deps.py[m
[1m+++ b/app/api/v1/deps.py[m
[36m@@ -266,30 +266,30 @@[m [mdef ensure_default_prompts(supabase: Client, project_id: int) -> None:[m
     """Ensure default prompt templates exist for a project."""[m
     defaults = [[m
         {[m
[31m-            "prompt_type": "reply",[m
[32m+[m[32m            "type": "reply",[m
             "name": "Helpful Reply",[m
             "system_prompt": "You write grounded Reddit replies that help first and pitch never.",[m
[31m-            "instructions": "Start with empathy, answer the actual question, avoid hard CTAs, and only mention the product when invited.",[m
[32m+[m[32m            "user_prompt_template": "Start with empathy, answer the actual question, avoid hard CTAs, and only mention the product when invited.",[m
         },[m
         {[m
[31m-            "prompt_type": "post",[m
[32m+[m[32m            "type": "post",[m
             "name": "Educational Post",[m
             "system_prompt": "You write Reddit posts that teach from direct experience.",[m
[31m-            "instructions": "Use first-hand lessons, concrete examples, and end with an invitation for discussion rather than a promo CTA.",[m
[32m+[m[32m            "user_prompt_template": "Use first-hand lessons, concrete examples, and end with an invitation for discussion rather than a promo CTA.",[m
         },[m
         {[m
[31m-            "prompt_type": "analysis",[m
[32m+[m[32m            "type": "analysis",[m
             "name": "Signal Review",[m
             "system_prompt": "You summarize opportunities with clarity and no fluff.",[m
[31m-            "instructions": "Highlight why the thread matters, what the risk is, and how the brand can contribute credibly.",[m
[32m+[m[32m            "user_prompt_template": "Highlight why the thread matters, what the risk is, and how the brand can contribute credibly.",[m
         },[m
     ][m
 [m
     existing = list_prompt_templates_for_project(supabase, project_id)[m
[31m-    existing_types = {p.get("prompt_type") or p.get("type") for p in existing}[m
[32m+[m[32m    existing_types = {p.get("type") for p in existing}[m
 [m
     for prompt in defaults:[m
[31m-        if prompt["prompt_type"] not in existing_types:[m
[32m+[m[32m        if prompt["type"] not in existing_types:[m
             prompt_data = {[m
                 **prompt,[m
                 "project_id": project_id,[m
